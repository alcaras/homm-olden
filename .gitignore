# Game install data — NEVER commit (copyrighted)
HeroesOldenEra_Data/

# Extracted game data (also copyrighted; regenerated from Core.zip)
catalog/raw/

# Python / Jekyll
__pycache__/
*.pyc
.DS_Store
_site/
.jekyll-cache/
.bundle/
vendor/
Gemfile.lock

# Local venv used for image extraction only
.venv/

# Local-only patch staging (raw Core.zip drops; extract into catalog/raw)
new_data/

# Editorial source notes (kept locally, not part of the published site)
notes-from-videos.md
discord-top5-and-faction-tier.md

# esbuild gets installed via npx; cache lives in node_modules
node_modules/

# Editor backup files
*~
dll/
